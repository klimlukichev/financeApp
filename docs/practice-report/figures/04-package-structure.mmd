flowchart TB
    root[ru.rsreu.klimlukichev.financeapp]

    subgraph ui [ui/]
        direction TB
        ui_home["home/ — HomeScreen, ViewModel"]
        ui_theme[theme/]
        ui_locale[locale/]
    end

    subgraph domain [domain/]
        direction TB
        dom_model[model/]
        dom_uc[usecase/]
        dom_repo[repository/]
        dom_cat[categorization/]
        dom_an[analytics/]
    end

    subgraph data [data/]
        direction TB
        data_local["local/ — Room, DAO, Entity"]
        data_rep[repository/]
        data_imp["importing/ — Parsers, PDF"]
    end

    subgraph di [di/]
        direction TB
        di_mod[AppModule.kt]
    end

    subgraph notif [notifications/]
        direction TB
        notif_wm["WorkManager, NotificationManager"]
    end

    root --> ui
    ui --> domain
    domain --> data
    data --> di
    di --> notif
